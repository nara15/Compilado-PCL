PROGRAM 
    VAR
    a,b : STRING;
    j : STRING;
    jose : STRING;
BEGIN 
  
    REPEAT 
        IF()
        IF()
        REPEAT
            IF()
        UNTIL
    UNTIL

    CASE variable OF
        1 : ;
    ELSE
        READ (a);
    
    READ (a)
   
    WRITE("Hola: ", jose, a, b)
   
END