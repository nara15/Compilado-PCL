PROGRAM 
    VAR
    a,b,c,d: INT;
    j : STRING;
    //jose, mario  STRING;
    nombres: ARRAY [1 .. 100] OF STRING;
    
    CONST
        PI = 3;
        NOMBRE = "Juan Pérez";
        NOMBRE = "Juan Pérez";
        APELLIDO = "Pérez";
        APELLIDO = 2.5;
  

    FUNCTION Saludar(): STRING
    BEGIN
       VAR a,b,c : STRING; 
    END

    FUNCTION fib(INT n):INT
    BEGIN
        VAR a : INT; 
    END

    
BEGIN 

    IF (hola) THEN
        BEGIN
            WRITE("Hola");
        END
    ELSE
        BEGIN
            IF (hola) THEN
            BEGIN
                WRITE("Hola otra vez");
            END
        END
        
    FOR i:=1 TO 10 DO
        BEGIN
            IF (hola) THEN
            BEGIN
                WRITE("Hola");
                
                WHILE hola DO
                    BEGIN
                        READ(a);
                        WRITE("HOLA:", a);
                    END
            END
            ELSE
                BEGIN
                    IF (hola) THEN
                    BEGIN
                        WRITE("Hola otra vez");
                    END
                END
        END
    
  
    REPEAT 
        REPEAT
            READ();
            a := 3 + 3 + 3;
        UNTIL ((2 + 2) MOD (2 + 3)) + (3*5/5)
    UNTIL saludar

    CASE variable OF
        1 : WRITE("Hola"); 
        2 : WRITE("dos");
       
    
        
    a := ((2 + 2) + (2 + b));
    b := 5*(6-3)+1;
    READ (a);
    WRITE("Hola: ", jose, a, b);
   
END